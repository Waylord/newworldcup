#include <stdio.h>
#include "../include/functions.h"


int main(){
    int group_matches = (groups * combin(teams / group));
    score result_group_matches[group_matches];
    char team_name[teams + 1 /* TBD option */][4]; //FIFA Trigramme list
    
}
